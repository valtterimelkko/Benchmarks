[project]
name = "benchmarks-dashboard"
version = "0.1.0"
description = "Private weekly LLM/agent benchmark dashboard"
requires-python = ">=3.12"
dependencies = ["requests", "beautifulsoup4", "openpyxl"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
